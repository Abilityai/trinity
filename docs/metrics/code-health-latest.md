# Code Health Report — 2026-08-18 (3fab94c7)

> Scope: `src/backend/` excluding `src/backend/venv/` (gitignored local virtualenv) and
> `src/backend/enterprise/` (private submodule — separate repo, invisible to parent-repo churn).
> Complexity via radon 6.0.1 (grade C+ blocks, summed per file); churn window: 90 days.
> Previous baseline: 2026-05-08 @ 1eb7b5e (a ~14-week gap — the weekly cadence lapsed, so
> ↑ trends partly reflect three months of accumulated growth, not one week's drift).

## Executive Summary

| Metric | Value | Trend vs 2026-05-08 |
|--------|-------|-------|
| Top hotspot score | 4743 (`services/agent_service/crud.py`) | ↓ (was 6105, `routers/chat.py`) |
| Files > 800 lines (critical) | 49 | ↑ (was 14) |
| Stale TODOs | 6 | → (was 6) |
| High fan-out files (>20 imports) | 9 | ↑ (was 4) |
| Circular imports | 0 | → (was 0) |

Secondary size counts: 98 files > 500 lines; 174 files > 300 lines.

The former #1 hotspot `routers/chat.py` (6105) collapsed to 216 — the #1483 chat split
(router → `chat_execution_service` / `dispatch_admission_service`) worked exactly as intended.
Its complexity moved into the service layer, where `task_execution_service.py` now carries the
highest raw CC in the backend (214). The size-violation and fan-out growth tracks three months
of feature landings (client_portal OSS-core, A2A outbound, skills multi-source, canary phases,
DB backups) without a corresponding extraction pass.

## Top 3 Hotspots (churn × complexity — highest refactoring ROI)

| Rank | File | Churn (90d) | CC Score | Hotspot Score | Lines | Trend |
|------|------|-------------|----------|---------------|-------|-------|
| 1 | `src/backend/services/agent_service/crud.py` | 31 | 153 | 4743 | 3206 | ↑ (was 2052) |
| 2 | `src/backend/services/task_execution_service.py` | 19 | 214 | 4066 | 2233 | ↑ (was 3171) |
| 3 | `src/backend/services/agent_service/lifecycle.py` | 28 | 139 | 3892 | 1702 | ↑ (was 742) |

Next in line: `routers/settings.py` 3381 (↑ from 735), `services/cleanup_service.py` 3168
(↑ from 1311), `services/skill_service.py` 2167 (new to top ranks), `routers/agents.py` 2100.

**Interpretation**: These files are both frequently changed and cognitively expensive.
Refactoring them reduces ongoing maintenance cost the most. All three sit on the agent
create/execute/recreate critical path, so their churn is structural, not incidental.

## Top 3 Size Violations

| File | Lines | Threshold Exceeded | Suggested Action |
|------|-------|--------------------|------------------|
| `src/backend/models.py` | 3855 | critical (>800) | Deliberate (Invariant #14 centralizes router models); low CC (flat Pydantic) — accept, or split by domain with a re-exporting facade if it keeps growing |
| `src/backend/db/migrations.py` | 3559 | critical (>800) | Frozen by design — retires with #746 (tables.py MetaData → autogenerated revisions); do not refactor |
| `src/backend/routers/settings.py` | 3269 | critical (>800) | Also hotspot #4 — split per-domain settings routers (template-registry, skills-library, brain-orb, elevenlabs, a2a-endpoints) into sub-routers mounted under one prefix |

49 files exceed the 800-line critical threshold (full list in git history of this file's
generating scan); the biggest genuinely-organic offenders after the three above:
`database.py` 3215, `services/agent_service/crud.py` 3206, `services/cleanup_service.py` 2729,
`client_portal/service.py` 2700.

## Top 3 Coupling Issues

| File | Import Count | Issue |
|------|--------------|-------|
| `src/backend/main.py` | 119 | Expected for the app-assembly module (router mounting); not actionable in isolation |
| `src/backend/database.py` | 51 | Facade over 27 domain operation classes — imports are the composition mechanism; watch, don't split |
| `src/backend/services/agent_service/crud.py` | 36 | Real fan-out on the hotspot #1 file — the phase-helper extraction (#1028) would take its imports with it |

9 files exceed 20 imports (was 4): the additions are `chat_execution_service.py` (28),
`routers/agent_data.py` (24), `skill_service.py` (23), `routers/a2a.py` (22),
`agent_service/lifecycle.py` (21), `task_execution_service.py` (28). No circular imports.

## Stale Smell Inventory

- **Total TODO/FIXME/HACK/XXX markers**: 6 (unchanged since May)
- Top smell-dense files: `services/monitoring_service.py` (2), `routers/ops.py` (2),
  `routers/monitoring.py` (1), `db/migrations.py` (1)
- All six are known deferrals (alerts table, error-rate calculation, Slack thread cleanup) —
  none is masking a live defect.

## Suggested Refactorings (Top 3 Hotspots)

1. **`services/agent_service/crud.py` (3206 lines, CC 153)** — execute the already-deferred
   #1028 follow-up: move the fenced `_*` creation-phase helpers (`_gate_tokenless_request`,
   `_stage_config_files`, `_apply_github_env`, `_materialize_agent_files`,
   `_reclaim_failed_creation_container`, …) into `services/agent_service/creation_phases.py`,
   leaving `create_agent_internal` as the thin orchestrator the #1484 refactor already shaped
   it into. Est. reduction: ~1500–1800 lines out of crud.py. Supports Invariant #1
   (router→service→db layering) and keeps the documented <100 SLOC / CC<20 per-helper budget
   reviewable.
2. **`services/task_execution_service.py` (2233 lines, CC 214 — highest raw CC in the
   backend)** — extract the terminal-envelope normalization (`_extract_agent_error`, the
   502/504 salvage paths, `sanitize_execution_log` wiring, status→error-code mapping) into a
   `services/terminal_envelope.py` leaf shared by `apply_result` and the #1083 callback path.
   Est. reduction: ~400–600 lines. Supports the single-terminal-applier direction
   (TARGET_ARCHITECTURE / #1081) by making the applier's input a typed value instead of
   inline parsing.
3. **`services/agent_service/lifecycle.py` (1702 lines, CC 139)** — split the nine
   `check_*_matches` recreate-drift predicates into `services/agent_service/drift_predicates.py`
   (pure functions over container attrs + DB rows), keeping `_apply_git_env_from_db` in place
   (its writer set is AST-pinned by `test_ent109_git_env_seam.py` — moving it breaks the
   guard). Est. reduction: ~400–500 lines. Supports Invariant #11 (Docker as source of truth)
   by making each predicate independently testable against labeled container fixtures.
