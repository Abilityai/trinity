# Release Coherence Review — v0.9.5 — VERDICTS CONFIRMED 2026-09-17

Confirmed by the operator 2026-09-17:
- **Bar 1 met by build.** The DO listing goes live after the cut (D1, notes line).
- **Bar 4:** ent#323 and ent#137 **move below the floor** (D2).
- **C4 FENCE** (F1).
- **Release is TODAY (2026-09-17).** Freeze = now. M1–M4 + F1 must clear today, in parallel, before `/release`.

Generated 2026-09-17 by /release-plan v1.1 (full mode). Payload: `v0.9.0..origin/dev`.
Base note: `git describe` on main returns `cli-v0.2.7` (a CLI tag); the product base is `v0.9.0` (93d7ce7c8, 2026-08-17).
`origin/main` is an ancestor of `origin/dev` (0 commits on main that dev lacks), so the release merge is a fast-forward.
Release candidates: rc1/rc2 were tagged on dev (09-02). rc3 was tagged on `feature/ent580-582-first-run` (09-11). rc4 was set on dev by #2838 (3a47f7440, 09-17).
Epic deep-dives used the batched sub-issue GraphQL fallback (reference.md), not per-epic `/roadmap`, because the payload touched too many epics for one call each. The data source is the same.

Payload: ~272 issues (146 public in-dev · 66 private in-dev · ~60 private closed-on-merge) · 868 commits · 1,532 files (+247,666 / −32,161) · 31 days
Public in-dev types: 112 bugs · 14 features · 13 refactors · 5 docs
Themes (in-dev union): ui-ux 97 · reliability 57 · devex 18 · security 15 · infrastructure 11 · monetization 8 · channels 2
Contract for this cut: **ent#441, "Release floor", 8 bars** (operator-ruled 2026-08-20, extended through 09-08).

## Payload census
- Clean (in-dev + in commits): 210 of 212
- Stale label (in-dev, no commits): 2
  - #2361: resolved by #2364 (merge revision 0045, merged 08-21). Close at the cut.
  - #1846: pingpong test seed flake; no commit references it. Verify, then groom.
- Label ahead of code: ent#500 is `status-in-dev`, but only the OSS seam (#2596) landed. The enterprise route does not exist at pin 08cd4dd or on any branch.
- Label behind code:
  - #1028 is in-progress, but #2487 merged 09-16 → in-dev.
  - #2240 is in-progress, but it was fixed by #2800/#2805 (0063 BIGINT) → close as dup.
  - #2571 is in-dev, but PR #2708 duplicates it and is still a draft (#2706 already landed it) → close the draft.
- Unlabeled (in commits, not in-dev): ~60 enterprise issues closed-on-merge. The enterprise tracker closes on merge, so they will be missing from the release notes unless the close-out list is built from submodule-pin ancestry (65182c1..08cd4dd) + closed-since-2026-08-18. ent#331 is in progress for exactly this.
- No-issue commits: 8 feat/fix subjects without a ref. They are sub-commits inside merged PR branches; no standalone shipped work.
- Enterprise pin 08cd4dd = enterprise main tip ✅

## Release floor (ent#441) — scored
| Bar | Workstream | State |
|---|---|---|
| 1 | One-click hosted | #2280 ✅ in-dev · DO listing **not live**. Build/catalog PRs #2471/#2522/#2680/#2707 are merged; vendor review is pending (ent#464). The listing consumes the published images, so it can only go live **after** the cut. |
| 2 | Workspace | ✅ Every named item is in-dev (#329 #364 #428-430 #438 #253 t#1927 #457 #458 #472 [#474 #475 #492 #608] #534-537 #547 #403 #551 #553-555 #557 #556 t#2617 t#2559 t#2579/2580/2582). Rail live-refresh children ent#532/#533 are still in progress (draft PRs #2709/#2713); the rail polls until then. |
| 3 | Telemetry | ✅ ent#437 in-dev · ent#190 closed 09-07 |
| 4 | First-run | t#2305 ✅ · ent#411 ✅ · t#848 ✅ · ent#319 ✅ · **ent#323 ❌** (design spec only; the issue itself says "direction for later") · **ent#137 ❌** (HELD 08-13: the branch empties the Library + unmet ship prerequisites; untouched since 09-02) |
| 5 | Journey + gating | ✅ #2336 #2246 #2335 #2337 #2338 #2339 in-dev · #2267 closed |
| 6 | Credential vault | ✅ ent#279 in-dev (PR-2 #2264 merged 09-03; `fetch_credential` MCP tool present) |
| 7 | Subscription usage | ✅ #471 · ent#259 · ent#434 in-dev. **But** #2419 misreads overage (see C2). |
| 8 | Tech debt | ✅ #2314 in-dev · #1028 (#2487 merged 09-16; relabel) |

## Coherence clusters (proposed verdicts)
| # | Cluster | Shipped | Missing | Visible? | Verdict | Action | Est |
|---|---|---|---|---|---|---|---|
| C1 | **dev is red** | #2487 (#1028 split, 09-16) + ent#615 tests (#2757, 09-15) | `backend-unit-test` has failed on both pushes since #2487 (5c4dc4b2a, 4196a6d52): 13 failures, all in `test_ent615_token_free_remotes.py` (`services.git_service` has no attribute `conflicts`/`token_scrub`). The file passes in isolation (89/89), so it is order-dependent `sys.modules` pollution: another test stubs the now-package `services.git_service`. rc4 was set on top of it. | CI | **COMPLETE** | Find the polluting stub (the lint job's pollution check passed, so it is a shape the lint misses) and fix it. Re-verify the full unit job. | 1–2h |
| C2 | Subscription headroom reads overage as ~1% | #2409 auto-switch ranks by headroom · ent#259 pressure tile · ent#434 ≥75% alert | #2419: `_parse_utilization` turns 1.2 (120%) into 1.2%. The overage subscription ranks as having the MOST headroom, the tile shows it nearly empty, and the ≥75% alert never fires for it. Still present on origin/dev L132. | YES (bar 7) | **COMPLETE** (guard half; never FENCE) | Treat the header as always a fraction; add a test for 1.2 → 120.0 | ~1h |
| C3 | Paid-wake toggle is agent-callable | `PUT /api/agents/{name}/operator-resume` (ent#329, #2312) | Gated by `OwnedAgentByName` only. An agent-scoped key resolves to its owner, so an agent can turn on its own (or a same-owner sibling's) paid wake-ups. That grants spend (the commit says "must not be the agent's to make"). Invariant #8 grant-vs-use. Confirmed by reading the code, not tested live. | no | **COMPLETE** | `reject_agent_principal(current_user)` in the PUT + unit test | ~1h |
| C4 | `get_agent_assignments` MCP tool has no backend | OSS seam + tool (#2596), advertised to every agent key | `GET /api/enterprise/assignments/agents/{name}` does not exist in any edition. The ent#500 enterprise half has no PR. The tool always degrades to `enabled:false`. | yes (every agent's tool list) | **FENCE** | Drop it from `toolGroups` until ent#500 lands; relabel ent#500 → in-progress | ~0.5h |
| C5 | Floor bar 1: DO listing | Everything buildable is merged | The listing going live (external vendor review; it needs the published 0.9.5 images) | n/a | **DEFER (post-cut)** — ruling needed | Operator: accept "listing goes live after the cut" as meeting bar 1 | 0 |
| C6 | Floor bar 4: ent#323, ent#137 | — | Neither can land this cut (ent#323 design-only; ent#137 held on a regression) | no (nothing shipped) | **DEFER** — ruling needed | Operator: move both below the floor | 0 |
| C7 | Getting-started launcher unverified | #2854 moved the checklist behind a header launcher (09-17). It renders nothing when unentitled, which is correct. | Never rendered anywhere: the endpoint 404s on the local stack and the rc3 droplet, and the component has no mount test. | yes (entitled builds) | **COMPLETE (verify only)** | Render it once on an enterprise-mounted local stack: launcher, popover, dismiss, Escape | 0.5h |
| C8 | Work card / rail live refresh | Rail shell + tabs, execution card | ent#620 (PR #2844, review required), ent#532/#533 (drafts #2709/#2713) | cosmetic (polls) | **SHOULD** — ship as increment | Review #2844 if it is ready before the cut; otherwise defer, no notes line needed | review |
| C9 | Agent-server conflict classifier drift | ent#615 added the `TRINITY_GIT_NO_CREDENTIAL` marker to backend `conflicts.py` | The agent-server copy `agent_server/utils/git_conflict.py` lacks it, and no two-copy parity test exists | unlikely (GIT_TERMINAL_PROMPT=0 usually adds "could not read Username") | **DEFER** | File an issue: add the pattern + parity test | 0 |
| C10 | Release-notes coverage | ~60 enterprise closures | Not in the in-dev census | notes | **COMPLETE (at cut)** | Build the enterprise close-out from `git -C src/backend/enterprise log 65182c1..<pin>` + closed-since-08-18 | 0.5h |
| C11 | Tracker hygiene | — | #2361 close · #2240 close (dup) · #1028 → in-dev · ent#500 → in-progress · close draft #2708 · #1846 verify · #2759/#2693 carry in-dev + ready (harmless) | no | /groom | `/groom` | 0.25h |

## Seam findings
- **B1/D (reachability)**, 80 new routes: 64 UI-only · 6 MCP-only · 8 both · 2 neither.
  - Fractures: C4 (tool calls a route that does not exist) and C3 (agent-callable grant).
  - Notes (invisible, defer):
    - `DELETE /api/rooms/{id}/participants/{agent}` has no caller in any surface (pre-existing; moved from enterprise).
    - The headroom-history route is backend-only by design (UI sparkline later; MCP in ent#351).
    - `# mcp:` headers are missing on `client_portal/asks/router.py`, `shared_sessions/router.py` and `routers/agents.py`, and incomplete on `routers/subscriptions.py`.
    - Four components call the API directly instead of through a store (AgentCanvasTab, ReliabilityPanel, RoomBudgetDefaultsPanel, CredentialVaultPanel with raw axios): Invariants #6/#7.
- **B2 (mirrors)**: all 5 byte-identical pairs are identical. The new ent#615 credential-helper mirror has a parity test. C9 is the only drift. The Invariant #5 list in architecture.md still names 4 mirrors (there are now 5, plus git_conflict.py) → docs follow-up.
- **C (migrations)**: parity guard PASS (23 SQLite ↔ 25 Alembic, 0039–0063).
  - One head on dev (0063), on main (0044), on the simulated merge (0063), and on enterprise (0017).
  - All 23 pairs match semantically.
  - Notes: `0053_user_ui_preferences` enforces an FK only on *upgraded* PostgreSQL, since a fresh PG build strips FKs (follow-up). `0063` is PG-only by design. A docstring in `migrations.py` says 0039 where it should say 0042.
  - Enterprise 0016/0017 have SQLite partners and do not ALTER any OSS table.
- **E (dark flags)**: no new `*_ENABLED` flag and no changed default in config/compose/.env.example. Removal of `WORKSPACE_ENABLED` is complete and guarded by a test. Dark by design:
  - the per-agent `operator_resume_enabled`
  - `operator_intake_consent_enabled`
  - the provenance-gated hardening guide
  - Tier-2 telemetry consent
  - the entitlement-gated modules (published images are built without the submodule, which is open-core as intended; public notes must not enumerate them)

## Self-declared incompleteness (open, not in payload)
- **Pull dispatch** (#1081 in progress; #1766 soak; P1 bugs #2842 #2843 #2845 #2846 filed 09-16). The floor says pull-by-default is not flipped this cut → Known limitation.
- **Subscriptions**: #2419 (→ C2) · #2420 (test wipes subscriptions on its target instance) · #2421 (subscription writes not audit-logged).
- **Operations feed**: #2377 (a lost respond race is not surfaced).
- **Agent-key breadth**: ent#629 (agent-scoped keys are owner-equivalent on raw chat/task/fan-out/loops/A2A; the sibling ent#628 is fixed in the payload) · ent#143 (accepted-risk umbrella). Tracked; no public notes line.
- #1028/#1030/#2290 refactor follow-ons, ent#178, ent#252: invisible.

## Soak risk
- **High.** 53 commits in the last 3 days; dev is red since 09-16.
- #2487 (a 4-module split of settings, git_service, agent_client and the ops/public handlers) landed 09-16, which is inside rc4. It is route-set-pinned by test, but it is the largest late structural change.
- #2854 (onboarding launcher) landed today, unrendered.
- Churn hot spots: `PortalConversation.vue` (56 commits), `client_portal/service.py` (55), `Portal.vue` (42), `stores/clientPortal.js` (39), `db/migrations.py` (48).
- The DO rc3 droplet was cut from a feature branch, not from dev.

---

## Code-Freeze Work Order — v0.9.5 (CONFIRMED)
Freeze: **2026-09-17 (today, operator ruling)** · Go/No-Go: **CONDITIONAL GO**. Cut only when all of the following hold:
- M1–M3 + F1 are merged to dev
- `backend-unit-test` is green on the dev tip
- M4 is verified

Parallelize: M1, M2, M3 and F1 are independent PRs with different owners, and each needs one review plus the required checks (~1h CI).
Fallbacks, if an item slips past the cut window:
- **M1:** no fallback. Do not cut on red.
- **M2 / M3:** guard halves, so they cannot be FENCEd. Hold the cut, don't ship without them.
- **M4:** if the launcher misbehaves on an entitled build, PULL #2854 (revert to the inline card); unentitled builds are unaffected.

### MUST LAND before freeze (4 items, ~0.5d)
| # | Repairs | Action | Verdict | Est | Owner | Issue |
|---|---|---|---|---|---|---|
| M1 | C1 dev red | Fix the order-dependent `git_service` stub pollution; full unit job green | COMPLETE | 1–2h | dolho (#2487 author) | new bug |
| M2 | C2 overage misread | `_parse_utilization` always reads a fraction + test | COMPLETE | 1h | webmixgamer (#2409 author) | #2419 |
| M3 | C3 paid-wake grant | `reject_agent_principal` on the PUT + test | COMPLETE | 1h | dolho (ent#329 author) | new bug |
| M4 | C7 launcher | Render-verify #2854 on an enterprise-mounted local stack | COMPLETE (verify) | 0.5h | obasilakis | #2854 |

### SHOULD LAND (droppable)
| # | Repairs | Action | Est | Owner |
|---|---|---|---|---|
| S1 | C8 | Review + merge #2844 (ent#620 Work card line) | review | dolho |
| S2 | C10 | Enterprise close-out list for the notes | 0.5h | release driver |
| S3 | C11 | `/groom` the stale labels | 0.25h | release driver |

### FENCE (gating + docs only)
| # | Cluster | Fence checklist | Est | Owner |
|---|---|---|---|---|
| F1 | C4 assignments tool | ☐ drop `createAssignmentTools` from `server.ts` toolGroups (or mark it `canAccess: false`) ☐ no release-note claim ☐ ent#500 → in-progress, AC: re-register the tool when the enterprise route lands ☐ no Known-limitations line (invisible once unregistered) | 0.5h | AndriiPasternak31 |

### DEFER (explicitly acknowledged)
| # | What's missing | Tracked | Notes line? |
|---|---|---|---|
| D1 | DO Marketplace listing live (floor bar 1) | #2281 / ent#464 | Yes, if the notes mention one-click hosting |
| D2 | ent#323 zero-commitment door, ent#137 archetype library (floor bar 4, **moved below the floor by the operator 09-17**) | ent#323 / ent#137 | No (nothing shipped) |
| D3 | Rail live refresh | ent#532 / ent#533 (drafts #2709/#2713) | No (polls) |
| D4 | Agent-server classifier marker + parity test | DEBT_INBOX `debt:2026-09-17-agent-server-conflict-classifier-missing-no-credential-marker` | No |
| D5 | Room participant removal has no surface | DEBT_INBOX `debt:2026-09-17-room-participant-removal-has-no-surface` | No |
| D6 | Fresh-vs-upgraded PG FK on user_ui_preferences | DEBT_INBOX `debt:2026-09-17-pg-user-ui-preferences-fk-fresh-vs-upgraded` | No |
| D7 | Invariant #5 mirror list, missing `# mcp:` headers, store-bypass components | DEBT_INBOX `debt:2026-09-17-v095-seam-tidy-mirror-list-mcp-headers-store-bypass` | No |
| D8 | Pull dispatch hardening | #1081 / #1766 / #2842–#2846 | Yes |
| D9 | Agent-key breadth on raw routes | ent#629 / ent#143 | No (security posture, tracked privately) |
| D10 | Subscription test wipes / audit logging | #2420 / #2421 | No |

### Release notes: Known limitations
```markdown
## Known limitations

- **Pull dispatch** remains opt-in and experimental. Push dispatch is the default; pull-by-default waits on a production soak (#1766) and the hardening in #2842–#2846.
- **One-click hosting on DigitalOcean**: the Marketplace image and first-boot flow ship in this release; the Marketplace listing itself goes live after vendor review (#2281).
- **Subscription headroom history** is recorded but not yet charted; the admin read is available over the API only.

## Upgrade notes

- `WORKSPACE_ENABLED` is retired and does nothing; you can delete it from `.env`.
- The bundled community skill source now pins `v0.2.0` for **new** installs only. Existing installs stay on `v0.1.0` until an admin re-points the source.
- An agent's owner must switch on "answer wakes the agent" per agent (Reliability panel) before an answered ask re-triggers it. It is off by default because each wake-up is a paid turn.
- New opt-in outbound calls, both off until an admin consents: operator intake contact, and Tier-2 telemetry sharing.
- Images from `docker-compose.hosted.yml` have the in-app bug-report widget built in (`VITE_BUG_REPORTING_ENABLED` is a build argument).
```

### Recommended remediation commands
- `/groom` → 6 label fixes (C11)
- `/implement` → M1, M2, M3
- `/sync-feature-flows` / docs → D7 (architecture Invariant #5 list)
- `/release` → after M1–M4 clear and dev is green
