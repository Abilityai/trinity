# CSO Diff Audit — 2026-09-24 — Issue #3012

**Mode**: daily (8/10 confidence gate)
**Scope**: branch diff (`feature/3012-model-unsupported-not-auth` vs `dev`)
**Diff**: 22 files (agent server, backend, scheduler mirror, base Dockerfile, docs, tests)

## Attack Surface Delta

| Category | Count |
|---|---|
| New endpoints | 0 |
| New WebSocket channels | 0 |
| New MCP tools | 0 |
| New public surfaces | 0 |
| New background jobs | 0 |
| New external integrations | 0 |

Error-classification change plus a base-image version pin. No new attack surface.

## Findings

**None.**

## Checked

- **Secrets (P2)**: diff scanned for `sk-`, `ghp_`, `gho_`, `github_pat_`, `xox[bp]-`, `AKIA` and secret assignments. No hits. No `.env` or workflow files changed.
- **Supply chain (P3)**: `@anthropic-ai/claude-code` moves from `@latest` to an exact `ARG CLAUDE_CODE_VERSION=2.1.281`. Closes the Claude Code half of cso-2026-07-13 F5 (unpinned agent-base runtime). The platform-native optional dependency is already reinstalled at the same resolved version. `@google/gemini-cli` stays unpinned (out of scope).
- **Dockerfile (P5)**: the new `ARG` carries a version, not a secret. `USER` directive unchanged.
- **Error text exposure (A01/A09)**: the agent's model-rejection message passes `sanitize_text` (`error_classifier._model_rejection_message`) before it reaches the 400 body, the execution row and `/chat`. External Workspace clients get fixed copy (`client_portal/service.py`, category `model_unsupported`); the CLI version sentence is never shown to them.
- **Auth boundaries (A01)**: no route or dependency changed. `is_auth_failure` now returns False on a model rejection. A real credential failure is unaffected: the stream `authentication_failed` / stderr auth-abort checks run first on the agent, and the backend markers are fixed phrases (`[claude-code:unrecognized_model]`, `does not support this model`, `model unsupported:`) that no auth message contains (negative tests in `tests/unit/test_3012_model_rejection.py`).
- **Credential state (STRIDE: EoP/Tampering)**: net reduction. A bad model setting can no longer move an agent onto another subscription or the platform API key (SUB-003 / #2638 fallback).

## Posture Delta — net positive

- Reproducible agent images for the Claude Code CLI.
- Fewer automatic credential-state changes on non-credential failures.
