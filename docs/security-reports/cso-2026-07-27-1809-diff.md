# CSO Audit — 2026-07-27 (diff mode, daily gate)

**Scope**: branch `feature/1809-image-drift-recreate` vs merge-base `e9eb40da` (dev) — the #1809 image-drift-recreate change.
**Mode**: daily (8/10 confidence gate) · all phases, diff-constrained.
**Result**: **0 findings.** 4 candidates evaluated; none survived the confidence gate / hard exclusions.

## Architecture delta (Phase 0)

The diff adds a read-only Docker-daemon comparison (`image_get`, executor-wrapped) feeding a ninth
`needs_recreation` predicate in `start_agent_internal`, gated to cold starts and non-ephemeral
agents; recreate gains race tolerance (NotFound/409), a version-label refresh, and reason
surfacing into the start response + audit event. No new endpoints, no auth changes, no schema
changes, no new secrets, no dependency or CI changes.

## Attack surface census (Phase 1, diff-scoped)

| Category | Delta |
|---|---|
| Unauthenticated endpoints | 0 |
| Authenticated endpoints | 0 new; 1 changed (`POST /api/agents/{name}/start` — response/audit fields only; auth unchanged: `AuthorizedAgentByName` + `enforce_agent_spawn_scope`) |
| File upload / WS / integrations / background jobs | 0 |
| CI/CD, Docker/compose, env | 0 |
| New internal capability | backend → local Docker daemon `images.get` (read-only; never pulls) |

## Phase results

- **P2 Secrets (diff)**: clean — the only pattern hits are the `check_github_pat_env_matches`
  *function name*. No credentials, no enterprise-design disclosure in changed docs
  (issue-number references only). No commits yet, so no history to scrub.
- **P3 Supply chain / P4 CI-CD / P5 infra / P6 webhooks / P8 skills**: no files in scope changed.
  Verified in-diff: `validate_base_image` (SEC-172 allowlist) still gates BOTH recreate paths
  (`lifecycle.py:442`, `:896`) before any image reference is used.
- **P7 LLM/AI**: no prompt, tool-schema, or LLM-output surface in the diff.
- **P9 OWASP**: A01 — no new routes, no enumeration split, mutation tier unchanged. A03 — the
  `images.get` argument is the container's own platform-written `Config.Image` (never request
  input); no SQL/shell. A08 — adoption resolves the **local** tag only (`images.get` cannot
  pull); retagging requires host/daemon access, which is a prerequisite strictly stronger than
  the attack it would enable. A09 — improved: recreate cause now audit-trailed
  (`recreated`/`recreate_reason`), drift and every fail-open exit logged.
- **P10 STRIDE (touched components)**: Repudiation improved (audit reason); Tampering requires
  pre-existing host compromise; Info disclosure limited to a boolean + enum reason visible to
  already-authorized agent accessors; Spoofing/EoP unchanged; DoS excluded by policy (and the
  Docker round-trip is lazy: cold-start + all-other-predicates-pass only).
- **P11 Data classification**: nothing new stored; image SHAs appear in INTERNAL logs only —
  never in API responses or audit rows.

## Filter stats (Phase 12)

4 candidates → 0 reported: (1) `recreate_reason` visible to shared accessors — confidence 3/10,
appendix-only (boolean + enum, no identifiers); (2) local-tag adoption trust — hard-excluded
(exploit prerequisite ⊇ impact); (3) fail-open masking — hard-excluded (hardening-without-vuln;
WARNING-logged by design); (4) 409-adoption container trust — refuted (the adopted name is
creatable only via the same authenticated platform path). No findings survived → no verifier
subagents spawned.

## Trend

Prior diff-mode reports exist (e.g. `cso-2026-06-28-1085-diff`); fingerprints are diff-local, so
no overlap computed. First audit of this branch.

> AI-assisted scan, not a substitute for a professional audit.
