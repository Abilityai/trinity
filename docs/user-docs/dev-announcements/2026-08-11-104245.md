---
date: 2026-08-11T10:42:45
channel: slack:updates
---

*v0.9.0 release plan (rev 2)* — plan PR #2096. Freeze Wed Aug 19. Headline: Workspace ships, skills library ships safely, schedules work again.

*Now — by Wed Aug 12* (dev is broken):
• PR #2095 — schedule enable/disable/trigger 422 regression (#2094, P0). CI green; needs a reviewer today.
• Enterprise pin bump → bce1175 (ent#356). ⚠️ Verify Alembic heads merge cleanly first — #2068 is exactly this failure mode (two heads → entitled instances silently degrade to OSS-only).
• Compose passthrough for `TRINITY_DEFAULT_SKILL_SOURCE(_REF)` (ent#237) — add to the backend's explicit env list, same pattern as `TRINITY_DEFAULT_SYSTEM_MANIFEST`.
• PR #2024 — approved + green, just merge it.

*Blocks the cut — by Wed Aug 19:*
• Security: ent#346 (P0, in progress) · ent#292 (P0 — PAT leak in sweeper logs, added; was missing from rev 1) · written ship/block decision on the ent#297/293/334 agent-key-admin cluster, recorded on #2096.
• Workspace core (all in progress): PR #2083 rename + one-click (ent#357 — green, needs review) · ent#375 sliding session renewal · ent#286 streaming · ent#362 workspace user as room participant (P1, added; was missing).
• In-flight bugs: ent#218 + ent#220 rooms edges · #1976 and #2076 — fix red CI, then merge · #2079 docs (green, needs review).

*Rides if ready — merged by freeze or moves whole to v0.9.1 (no partial stories):*
• #2042 chassis (changes requested + red CI) + ent#99/ent#100 tiles
• #736 + ent#178 — A2A bidirectional
• ent#137 — archetype library
• ent#242 + ent#342 — skill-runner surface + per-agent toggles
• ent#21 + ent#29 — client access controls
• ent#358/359/360/361 — Workspace IA polish
• #925 + #1198 — cron validation + schedule reconcile
• #1669 · #1677 · #1833 · #1834 — bug tail"""

payload = json.dumps({
    'channel': env['ANNOUNCE_SLACK_UPDATES_CHANNEL'],
    'text': MESSAGE
}).encode()

req = urllib.request.Request(
    'https://slack.com/api/chat.postMessage',
    data=payload,
    headers={
        'Authorization': f"Bearer {env['ANNOUNCE_SLACK_TOKEN']}",
        'Content-Type': 'application/json'
    }
)
resp = urllib.request.urlopen(req)
result = json.loads(resp.read().decode())
if result.get('ok'):
    print(f"SLACK: ok ts={result.get('ts')}")
else:
    print(f"SLACK: failed — {result.get('error', 'unknown')}")
